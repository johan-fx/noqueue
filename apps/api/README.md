# API Worker

This Hono Worker owns server-side behavior and Cloudflare bindings. Use root commands for development and checks. `wrangler.jsonc` contains local placeholders only; follow [`docs/runbooks/cloudflare-provisioning.md`](../../docs/runbooks/cloudflare-provisioning.md) before any remote operation.
