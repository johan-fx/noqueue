# Web application

This is the single React/Vite application used by browsers and packaged by Capacitor. Run it from the repository root with `pnpm dev`; see [`docs/architecture/runtime-channels.md`](../../docs/architecture/runtime-channels.md) before adding platform-specific behavior.
